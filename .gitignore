# Environment & Cache
.venv/
__pycache__/
*.pyc
*.pyo

# OS Files
.DS_Store
Thumbs.db

# Generated Data & Reports
# (These are generated by the pipeline scripts, no need to push them)
data/*.csv
reports/*.txt
scored_sessions.csv

# NOTE: The models/ folder is NOT ignored so the React Native app can pull the .tflite file.
pipeline/*.csv
pipeline/*.txt

pipeline/*.csv
pipeline/*.txt

.env 